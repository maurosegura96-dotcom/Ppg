# EXPO_PUBLIC_ prefix auto-exposes this to the app bundle (Expo SDK 49+).
# Same key as apps/web; restrict allowed domains/bundle IDs in the MapTiler
# dashboard before shipping to production.
EXPO_PUBLIC_MAPTILER_KEY=
