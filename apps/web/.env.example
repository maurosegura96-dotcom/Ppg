# Public by design: MapLibre GL fetches tiles directly from MapTiler's CDN
# using this key in the browser. Restrict allowed domains in the MapTiler
# dashboard (Account -> Keys -> Allowed URLs) before shipping to production.
NEXT_PUBLIC_MAPTILER_KEY=
