# apps/web — AeroParamotor OS PWA

Pendiente de scaffolding: Next.js 15 (App Router) + Service Worker (Workbox) +
MapLibre GL JS, consumiendo `@aeroparamotor/core` para tipos y algoritmos compartidos.

Próximo paso: `npx create-next-app@latest . --typescript --app` dentro de este directorio,
luego enlazar el workspace `@aeroparamotor/core`.
